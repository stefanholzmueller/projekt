{
  "private": true,
  "scripts": {
    "postinstall": "bower install",
    "b": "pulp browserify --to dist/app.js",
    "build": "pulp browserify --optimise --transform uglifyify --main Frontend --to dist/app.js",
    "watch": "pulp -w browserify --to dist/app.js"
  },
  "dependencies": {
    "virtual-dom": "^2.1.1"
  },
  "devDependencies": {
    "bower": "^1.8.0",
    "pulp": "^9.0.1",
    "purescript": "^0.10.1",
    "purescript-psa": "^0.3.9",
    "uglifyify": "^3.0.4"
  }
}
